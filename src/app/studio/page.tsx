import { Studio } from "@/components/studio";

export const metadata = {
  title: "Studio — VOXEMBLY",
  description: "Push to Think: every dictation becomes a typed, versioned, executable Thoughtform.",
};

export default function StudioPage() {
  return <Studio />;
}
